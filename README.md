# 📚 Каталог книг

Современное веб-приложение для управления каталогом книг с административной панелью и интерактивным чатом. Проект реализован с использованием React, Vite, Zustand, React Query и Socket.IO.

## 🚀 Быстрый старт

### Требования

- Node.js версии 18 или выше
- npm или yarn

### Установка

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd Catologist_of_books
```

2. Установите зависимости:

```bash
npm install
```

3. Запустите проект в режиме разработки:

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist/`

### Предварительный просмотр продакшен-сборки

```bash
npm run preview
```

## 📋 Скрипты

- `npm run dev` - Запуск dev-сервера
- `npm run build` - Сборка проекта для продакшена
- `npm run preview` - Предварительный просмотр продакшен-сборки
- `npm run lint` - Проверка кода линтером

## 🚀 Деплой на Netlify

Проект настроен для деплоя на Netlify. Netlify обеспечивает доступ к внешним API и WebSocket соединениям, в отличие от GitHub Pages.

### Автоматический деплой через Netlify

1. **Подключите репозиторий к Netlify:**
   - Зайдите на [netlify.com](https://www.netlify.com/)
   - Войдите через GitHub
   - Нажмите **"Add new site"** → **"Import an existing project"**
   - Выберите ваш репозиторий `Catologist_of_books`

2. **Настройки деплоя (уже настроены в `netlify.toml`):**
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Node version: `20`

3. **SPA роутинг:**
   - Настроен автоматически через `public/_redirects` и `netlify.toml`
   - Все маршруты будут корректно работать

4. **Переменные окружения (если нужно):**
   - В настройках сайта на Netlify можно добавить переменные окружения:
     - `VITE_API_BASE_URL` - базовый URL API
     - `VITE_WS_SOCKET_IO` - URL WebSocket сервера

### Ручной деплой через Netlify CLI

1. Установите Netlify CLI:
```bash
npm install -g netlify-cli
```

2. Войдите в Netlify:
```bash
netlify login
```

3. Инициализируйте сайт:
```bash
netlify init
```

4. Деплой:
```bash
netlify deploy --prod
```

## 🏗️ Архитектура проекта

```
Catologist_of_books/
├── src/
│   ├── components/          # Переиспользуемые компоненты
│   │   ├── Book/           # Компоненты для работы с книгами
│   │   ├── Layout/         # Компоненты макета
│   │   └── UI/             # UI-компоненты (Button, Input, Modal и т.д.)
│   ├── config/             # Конфигурационные файлы
│   │   └── api.js          # Конфигурация API
│   ├── pages/              # Страницы приложения
│   │   ├── HomePage.jsx    # Главная страница
│   │   ├── BookDetailPage.jsx  # Страница детальной информации о книге
│   │   ├── AdminPage.jsx   # Административная панель
│   │   └── ChatPage.jsx    # Страница чата
│   ├── services/           # Сервисный слой
│   │   └── api.js         # API-клиент
│   ├── stores/             # Zustand stores
│   │   ├── useBookStore.js # Store для книг и фильтров
│   │   └── useChatStore.js # Store для чата
│   ├── App.jsx             # Главный компонент приложения
│   ├── main.jsx            # Точка входа
│   └── index.css           # Глобальные стили
├── minecraft.ttf           # Кастомный шрифт
├── index.html              # HTML-шаблон
├── package.json            # Зависимости проекта
└── vite.config.js          # Конфигурация Vite
```

## 🛠️ Технологический стек

### Основные технологии

- **React 18** - UI-библиотека
- **Vite** - Сборщик и dev-сервер
- **React Router DOM** - Маршрутизация
- **Zustand** - Управление состоянием
- **React Query (TanStack Query)** - Управление серверным состоянием и кеширование
- **Socket.IO Client** - WebSocket-коммуникация для чата
- **Axios** - HTTP-клиент
- **GSAP** - Анимации
- **React Hot Toast** - Уведомления

### Backend API

Проект использует внешний backend-сервис:

- **REST API**: `http://158.160.203.172:8080`
- **Socket.IO**: `ws://158.160.203.172:8081`
- **WebSocket**: `ws://158.160.203.172:8082`
- **Документация**: `http://158.160.203.172:8083`

## 📱 Основной функционал

### Главная страница (`/`)

- Отображение полного списка книг
- Поиск книг в реальном времени
- Фильтрация по жанру, автору и году издания
- Сортировка (по названию, дате добавления, рейтингу)
- Бесконечный скролл (пагинация)
- Блок "Недавно просмотренные книги"

### Страница детальной информации о книге (`/book?id=N`)

- Полная информация о книге
- Навигация "хлебные крошки"
- Блок "Похожие книги" (по жанру и автору)
- Автоматическое сохранение в историю просмотров

### Административная панель (`/admin`)

- **CRUD-операции для книг:**
  - Создание, редактирование, удаление
  - Загрузка обложек (FormData)
  - Превью изображений
  - Мультиселект авторов и жанров
- **CRUD-операции для авторов:**
  - Создание, редактирование, удаление
- **CRUD-операции для жанров:**

  - Создание, редактирование, удаление

- Валидация форм
- Диалоги подтверждения удаления

### Чат (`/chat`)

- Интерактивный чат на Socket.IO
- Отображение статуса подключения
- Счетчик онлайн-пользователей
- История сообщений
- Поддержка ссылок на книги с превью-карточками
- Уведомления о новых сообщениях

## 🎨 Дизайн

Проект использует современный дизайн с элементами:

- **Glassmorphism** - стеклянный морфизм для карточек и модальных окон
- **Градиенты** - акцентные градиенты в кнопках и заголовках
- **Темная тема** - контрастная цветовая схема
- **GSAP-анимации** - плавные анимации появления элементов
- **Адаптивный дизайн** - полная поддержка мобильных устройств
- **Кастомный шрифт** - использование шрифта Minecraft для всего интерфейса

## 🔧 Конфигурация

### API Endpoints

Конфигурация API находится в `src/config/api.js`:

```javascript
export const API_CONFIG = {
  REST_API: "http://158.160.203.172:8080",
  WS_SOCKET_IO: "ws://158.160.203.172:8081",
  WS_WEBSOCKET: "ws://158.160.203.172:8082",
  DOCS: "http://158.160.203.172:8083",
};
```

### Переменные окружения

Для изменения API endpoints создайте файл `.env`:

```env
VITE_API_BASE_URL=http://158.160.203.172:8080
VITE_WS_SOCKET_IO=ws://158.160.203.172:8081
```

## 📦 Альтернативные способы деплоя

### Vercel

1. Установите Vercel CLI:

```bash
npm i -g vercel
```

2. Деплой:

```bash
vercel
```

### GitHub Pages (устаревший способ)

> ⚠️ **Внимание:** GitHub Pages не поддерживает доступ к внешним API и WebSocket соединениям. Рекомендуется использовать Netlify.

Если всё же нужно использовать GitHub Pages, workflow настроен в `.github/workflows/deploy.yml`.

## 🧪 Тестирование

Для запуска линтера:

```bash
npm run lint
```

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 👥 Автор

Проект разработан в рамках учебного задания для закрепления навыков работы с REST API, WebSocket/Socket.IO, управлением состоянием и сложными фронтенд-интерфейсами.

## 🔗 Полезные ссылки

- [Документация React](https://react.dev/)
- [Документация Vite](https://vitejs.dev/)
- [Документация Zustand](https://zustand-demo.pmnd.rs/)
- [Документация React Query](https://tanstack.com/query/latest)
- [Документация Socket.IO](https://socket.io/docs/v4/)

---

Для подробного описания функционала см. [FUNCTIONALITY.md](./FUNCTIONALITY.md)
