# 🚀 Инструкция по деплою на Netlify

## Быстрый старт

### Вариант 1: Через веб-интерфейс Netlify (рекомендуется)

1. **Зайдите на [netlify.com](https://www.netlify.com/)**
   - Войдите через GitHub аккаунт

2. **Подключите репозиторий:**
   - Нажмите **"Add new site"** → **"Import an existing project"**
   - Выберите **GitHub** как провайдера
   - Найдите репозиторий `Catologist_of_books`
   - Нажмите **"Import"**

3. **Настройки деплоя (уже настроены автоматически):**
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Node version: `20`
   
   > Все настройки уже указаны в файле `netlify.toml`, поэтому Netlify автоматически их подхватит.

4. **Нажмите "Deploy site"**

5. **Готово!** 
   - Netlify автоматически задеплоит сайт
   - Каждый push в `main` ветку будет автоматически триггерить новый деплой

### Вариант 2: Через Netlify CLI

1. **Установите Netlify CLI:**
```bash
npm install -g netlify-cli
```

2. **Войдите в Netlify:**
```bash
netlify login
```

3. **Инициализируйте сайт:**
```bash
netlify init
```
   - Выберите "Create & configure a new site"
   - Выберите команду для сборки: `npm run build`
   - Выберите директорию для публикации: `dist`

4. **Деплой:**
```bash
netlify deploy --prod
```

## Переменные окружения (опционально)

Если нужно изменить API endpoints, добавьте переменные окружения в настройках сайта на Netlify:

1. Зайдите в **Site settings** → **Environment variables**
2. Добавьте:
   - `VITE_API_BASE_URL` = `http://158.160.203.172:8080`
   - `VITE_WS_SOCKET_IO` = `ws://158.160.203.172:8081`

> По умолчанию используются значения из `src/config/api.js`

## SPA роутинг

SPA роутинг настроен автоматически через:
- `netlify.toml` - конфигурация редиректов
- `public/_redirects` - файл редиректов для Netlify

Все маршруты (`/`, `/admin`, `/book?id=1` и т.д.) будут работать корректно.

## Преимущества Netlify над GitHub Pages

✅ **Доступ к внешним API** - Netlify не блокирует запросы к внешним API  
✅ **WebSocket поддержка** - Полная поддержка WebSocket соединений  
✅ **Автоматический HTTPS** - Бесплатный SSL сертификат  
✅ **CDN** - Быстрая загрузка по всему миру  
✅ **Автоматический деплой** - При каждом push в репозиторий  
✅ **Preview deployments** - Предпросмотр для pull requests  

## Полезные команды

```bash
# Предпросмотр деплоя (без публикации)
netlify deploy

# Публикация в продакшен
netlify deploy --prod

# Открыть сайт в браузере
netlify open

# Просмотр логов
netlify logs
```

## Troubleshooting

### Проблема: Сайт не собирается

- Проверьте, что Node.js версии 20 установлен
- Проверьте логи сборки в Netlify Dashboard → Deploys → [выберите деплой] → Build log

### Проблема: API запросы не работают

- Убедитесь, что API сервер доступен из интернета
- Проверьте CORS настройки на API сервере
- Проверьте переменные окружения в Netlify

### Проблема: WebSocket не подключается

- Убедитесь, что WebSocket сервер доступен из интернета
- Проверьте, что используется правильный URL (ws:// или wss://)
- Netlify поддерживает WebSocket, но убедитесь, что сервер тоже доступен
