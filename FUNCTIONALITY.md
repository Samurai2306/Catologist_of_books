# 📖 Описание функционала приложения "Каталог книг"

## Содержание

1. [Общая архитектура](#общая-архитектура)
2. [Главная страница](#главная-страница)
3. [Страница детальной информации о книге](#страница-детальной-информации-о-книге)
4. [Административная панель](#административная-панель)
5. [Чат](#чат)
6. [Управление состоянием](#управление-состоянием)
7. [API интеграция](#api-интеграция)
8. [Дизайн и анимации](#дизайн-и-анимации)

---

## Общая архитектура

### Структура приложения

Приложение построено на React с использованием следующих паттернов:

- **Компонентный подход** - разделение на переиспользуемые компоненты
- **Сервисный слой** - вынесение API-запросов в отдельный модуль
- **Централизованное управление состоянием** - Zustand для клиентского состояния
- **Кеширование запросов** - React Query для серверного состояния
- **Маршрутизация** - React Router для навигации

### Поток данных

```
UI Component → Store (Zustand) → API Service → Backend
                ↓
         React Query Cache
```

---

## Главная страница

### Компонент: `HomePage.jsx`

#### Основной функционал

1. **Загрузка данных**
   - Использует React Query для получения списка книг
   - Кеширование на 5 минут
   - Автоматическая повторная загрузка при ошибках

2. **Фильтрация книг**

   **Поиск по тексту:**
   - Поиск по названию книги
   - Поиск по имени автора
   - Debounce 300ms для оптимизации

   **Фильтр по жанру:**
   - Выбор из списка всех доступных жанров
   - Фильтрация происходит на клиенте

   **Фильтр по автору:**
   - Выбор из списка всех авторов
   - Поддержка множественных авторов у книги

   **Фильтр по году издания:**
   - Динамическое формирование списка годов из существующих книг
   - Сортировка по убыванию (новые сначала)

3. **Сортировка**

   Доступные варианты:
   - По названию (алфавитный порядок)
   - По дате добавления (новые сначала)
   - По рейтингу (высокий рейтинг сначала)

4. **Пагинация (бесконечный скролл)**

   - Использует Intersection Observer API
   - Загружает по 12 книг за раз
   - Автоматическая подгрузка при достижении конца списка
   - Индикатор загрузки

5. **История просмотров**

   - Сохранение в `localStorage` через Zustand persist
   - Отображение последних 6 просмотренных книг
   - Автоматическое обновление при просмотре новой книги

#### Компоненты

- `BookFilters` - панель фильтров и поиска
- `BookCard` - карточка книги с GSAP-анимацией
- `LoadingSpinner` - индикатор загрузки

---

## Страница детальной информации о книге

### Компонент: `BookDetailPage.jsx`

#### Основной функционал

1. **Получение данных о книге**

   - Чтение параметра `id` из query-строки URL
   - Загрузка через React Query с кешированием
   - Обработка ошибок (404, 500 и т.д.)

2. **Отображение информации**

   - Название книги
   - Авторы (поддержка множественных)
   - Жанры (поддержка множественных)
   - Год издания
   - Рейтинг
   - Описание
   - Обложка (с fallback на placeholder)

3. **Навигация "хлебные крошки"**

   Структура: `Главная > Жанр > Название книги`
   - Ссылка на главную страницу
   - Отображение первого жанра (если есть)
   - Текущее название книги

4. **Похожие книги**

   Алгоритм поиска:
   - Поиск книг с совпадающими жанрами
   - Поиск книг с совпадающими авторами
   - Исключение текущей книги
   - Ограничение до 6 книг

5. **Сохранение в историю**

   - Автоматическое добавление в историю просмотров
   - Обновление при каждом просмотре
   - Сохранение в `localStorage`

---

## Административная панель

### Компонент: `AdminPage.jsx`

#### Структура

Панель разделена на три вкладки:
- **Книги** - управление книгами
- **Авторы** - управление авторами
- **Жанры** - управление жанрами

#### CRUD-операции для книг

**Создание книги:**

1. Открытие модального окна с формой
2. Заполнение полей:
   - Название (обязательное)
   - Описание
   - Год издания (число)
   - Рейтинг (0-10, число)
   - Авторы (мультиселект)
   - Жанры (мультиселект)
   - Обложка (файл или URL)

3. Валидация:
   - Проверка обязательных полей
   - Проверка формата года
   - Проверка диапазона рейтинга

4. Загрузка изображения:
   - Если выбран файл - загрузка через FormData
   - Если указан URL - использование URL
   - Превью перед загрузкой

5. Отправка данных:
   - POST запрос на `/books`
   - Обновление кеша React Query
   - Уведомление об успехе/ошибке

**Редактирование книги:**

1. Предзаполнение формы данными книги
2. Те же шаги валидации и загрузки
3. PUT запрос на `/books/{id}`
4. Обновление кеша

**Удаление книги:**

1. Диалог подтверждения
2. DELETE запрос на `/books/{id}`
3. Обновление списка

#### CRUD-операции для авторов

**Создание:**
- Поле: название (обязательное)
- POST на `/authors`

**Редактирование:**
- PUT на `/authors/{id}`

**Удаление:**
- DELETE на `/authors/{id}`

#### CRUD-операции для жанров

Аналогично авторам:
- Создание, редактирование, удаление
- Поле: название

#### Валидация форм

**Клиентская валидация:**

```javascript
- Название книги: обязательное поле, не пустое
- Год издания: число (если указано)
- Рейтинг: число от 0 до 10 (если указано)
- Название автора/жанра: обязательное поле
```

**Отображение ошибок:**
- Подсветка поля с ошибкой
- Текстовое сообщение об ошибке
- Блокировка отправки формы

#### Диалоги подтверждения

- Модальное окно с вопросом
- Отображение названия удаляемого элемента
- Предупреждение о необратимости действия
- Кнопки "Отмена" и "Удалить"

---

## Чат

### Компонент: `ChatPage.jsx`

#### Архитектура

Использует Socket.IO для real-time коммуникации:

```
Client → Socket.IO Client → Socket.IO Server → Broadcast → All Clients
```

#### Функционал

1. **Подключение**

   - Ввод имени пользователя
   - Подключение к `ws://158.160.203.172:8081`
   - Автоматическое переподключение при разрыве

2. **Отправка сообщений**

   - Ввод текста в поле
   - Отправка через `message:send` event
   - Передача имени пользователя
   - Очистка поля после отправки

3. **Получение сообщений**

   - Слушатель события `message:new`
   - Добавление в массив сообщений
   - Автоматическая прокрутка к новому сообщению

4. **История сообщений**

   - Загрузка при подключении через `messages:history`
   - Отображение всех предыдущих сообщений

5. **Статус подключения**

   - Индикатор онлайн/оффлайн
   - Анимация пульсации для онлайн-статуса
   - Блокировка отправки при отключении

6. **Онлайн-пользователи**

   - Счетчик через событие `users:online`
   - Отображение количества активных пользователей

7. **Ссылки на книги**

   - Автоматическое определение ссылок вида `/book?id=N`
   - Загрузка информации о книге
   - Отображение превью-карточки:
     - Обложка
     - Название
     - Авторы
   - Кликабельная ссылка на страницу книги

#### Компоненты

- `ChatMessage` - отдельное сообщение
- `BookPreview` - превью книги в сообщении

#### Уведомления

- Интеграция с `react-hot-toast`
- Уведомления о подключении/отключении
- Уведомления об ошибках
- Счетчик непрочитанных сообщений в навигации

---

## Управление состоянием

### Zustand Stores

#### `useBookStore.js`

**Назначение:** Управление состоянием книг и фильтров

**Состояние:**

```javascript
{
  viewedBooks: [],           // История просмотров (ID книг)
  searchQuery: '',           // Текст поиска
  selectedGenre: null,       // Выбранный жанр (ID)
  selectedAuthor: null,      // Выбранный автор (ID)
  selectedYear: null,       // Выбранный год
  sortBy: 'title',           // Тип сортировки
}
```

**Методы:**

- `addViewedBook(bookId)` - добавление в историю
- `setSearchQuery(query)` - установка поискового запроса
- `setSelectedGenre(genre)` - выбор жанра
- `setSelectedAuthor(author)` - выбор автора
- `setSelectedYear(year)` - выбор года
- `setSortBy(sort)` - установка сортировки
- `resetFilters()` - сброс всех фильтров

**Персистентность:**

- Сохранение `viewedBooks` в `localStorage`
- Автоматическое восстановление при загрузке

#### `useChatStore.js`

**Назначение:** Управление состоянием чата

**Состояние:**

```javascript
{
  messages: [],              // Массив сообщений
  isConnected: false,        // Статус подключения
  onlineUsers: [],           // Список онлайн-пользователей
  unreadCount: 0,            // Счетчик непрочитанных
}
```

**Методы:**

- `addMessage(message)` - добавление сообщения
- `setMessages(messages)` - установка всех сообщений
- `setConnected(status)` - установка статуса подключения
- `setOnlineUsers(users)` - установка списка пользователей
- `resetUnreadCount()` - сброс счетчика
- `clearMessages()` - очистка сообщений

### React Query

**Назначение:** Управление серверным состоянием и кеширование

**Конфигурация:**

```javascript
{
  refetchOnWindowFocus: false,  // Не обновлять при фокусе
  retry: 1,                      // 1 попытка повтора
  staleTime: 5 * 60 * 1000,     // 5 минут кеширования
}
```

**Query Keys:**

- `['books']` - список всех книг
- `['book', id]` - конкретная книга
- `['authors']` - список авторов
- `['genres']` - список жанров

**Инвалидация:**

- Автоматическая после мутаций (create, update, delete)
- Ручная через `queryClient.invalidateQueries()`

---

## API интеграция

### Сервисный слой: `services/api.js`

#### Структура

Использует Axios для HTTP-запросов:

```javascript
const apiClient = axios.create({
  baseURL: API_CONFIG.REST_API,
  headers: {
    'Content-Type': 'application/json',
  },
})
```

#### Интерцепторы

**Обработка ошибок:**

```javascript
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    const message = error.response?.data?.message || error.message || 'Произошла ошибка'
    return Promise.reject(new Error(message))
  }
)
```

#### API методы

**Книги (`booksAPI`):**

- `getAll()` - GET `/books`
- `getById(id)` - GET `/books/{id}`
- `create(data)` - POST `/books`
- `update(id, data)` - PUT `/books/{id}`
- `delete(id)` - DELETE `/books/{id}`

**Авторы (`authorsAPI`):**

- `getAll()` - GET `/authors`
- `getById(id)` - GET `/authors/{id}`
- `create(data)` - POST `/authors`
- `update(id, data)` - PUT `/authors/{id}`
- `delete(id)` - DELETE `/authors/{id}`

**Жанры (`genresAPI`):**

- Аналогично авторам

**Изображения (`imagesAPI`):**

- `upload(formData)` - POST `/image/url`
  - Использует `multipart/form-data`
  - Возвращает URL загруженного изображения

### Socket.IO интеграция

**Подключение:**

```javascript
const socket = io(API_CONFIG.WS_SOCKET_IO, {
  transports: ['websocket'],
  reconnection: true,
  reconnectionDelay: 1000,
  reconnectionAttempts: 5,
})
```

**События:**

**Отправка:**
- `user:join` - присоединение пользователя
- `message:send` - отправка сообщения

**Получение:**
- `connect` - успешное подключение
- `disconnect` - отключение
- `message:new` - новое сообщение
- `messages:history` - история сообщений
- `users:online` - список онлайн-пользователей
- `error` - ошибка

---

## Дизайн и анимации

### Цветовая схема

**CSS переменные:**

```css
--bg-primary: #0a0a0f              /* Основной фон */
--bg-secondary: rgba(20, 20, 30, 0.8)  /* Вторичный фон */
--bg-glass: rgba(255, 255, 255, 0.05)   /* Стеклянный эффект */
--text-primary: #ffffff            /* Основной текст */
--text-secondary: rgba(255, 255, 255, 0.7)  /* Вторичный текст */
--accent-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%)
--accent-secondary: linear-gradient(135deg, #f093fb 0%, #f5576c 100%)
```

### Glassmorphism

**Применение:**

- Карточки книг
- Модальные окна
- Панель фильтров
- Хедер и футер

**Реализация:**

```css
.glass {
  background: var(--bg-glass);
  backdrop-filter: blur(20px);
  border: 1px solid var(--border-color);
  border-radius: 16px;
}
```

### GSAP анимации

**Анимация появления карточек:**

```javascript
gsap.fromTo(
  cardRef.current,
  { opacity: 0, y: 30, scale: 0.9 },
  { opacity: 1, y: 0, scale: 1, duration: 0.5, ease: 'power2.out' }
)
```

**Применение:**

- Карточки книг при загрузке
- Плавные переходы между страницами
- Hover-эффекты

### Адаптивность

**Breakpoints:**

- Desktop: > 768px
- Tablet: 768px - 968px
- Mobile: < 768px

**Адаптивные изменения:**

- Сетка книг: от 4-5 колонок до 1-2
- Навигация: горизонтальная → вертикальная
- Фильтры: сетка → колонка
- Модальные окна: полная ширина на мобильных

### Шрифт

**Использование кастомного шрифта:**

- **Шрифт:** `Minecraft` (minecraft.ttf)
- **Расположение:** корень проекта (`/minecraft.ttf`)
- **Подключение:** через `@font-face` в `src/index.css`
- **Fallback:** системные шрифты (для случаев, когда шрифт не загрузился)
- **Применение:** все текстовые элементы интерфейса
- **Оптимизация:** `font-display: swap` для быстрой отрисовки текста

**Реализация:**

```css
@font-face {
  font-family: 'Minecraft';
  src: url('/minecraft.ttf') format('truetype');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}

body {
  font-family: 'Minecraft', -apple-system, BlinkMacSystemFont, ...;
}
```

---

## Обработка ошибок

### Типы ошибок

1. **Сетевые ошибки**
   - Таймаут запроса
   - Отсутствие соединения
   - Ошибка сервера (500, 503)

2. **Ошибки API**
   - 404 - ресурс не найден
   - 400 - неверный запрос
   - 401/403 - проблемы авторизации

3. **Ошибки валидации**
   - Неверный формат данных
   - Отсутствие обязательных полей

### Обработка

**В компонентах:**

```javascript
if (error) {
  return (
    <div className="error-container">
      <h2>Ошибка загрузки</h2>
      <p>{error.message}</p>
      <button onClick={() => refetch()}>Попробовать снова</button>
    </div>
  )
}
```

**Уведомления:**

- Toast-уведомления через `react-hot-toast`
- Стилизация под темную тему
- Автоматическое скрытие через 3 секунды

---

## Производительность

### Оптимизации

1. **Кеширование**
   - React Query кеш на 5 минут
   - Локальное кеширование в Zustand

2. **Ленивая загрузка**
   - Code splitting для маршрутов
   - Lazy loading изображений
   - Suspense для асинхронных компонентов

3. **Debounce**
   - Поиск с задержкой 300ms
   - Оптимизация количества запросов

4. **Виртуализация**
   - Бесконечный скролл вместо полной загрузки
   - Отображение только видимых элементов

5. **Оптимизация анимаций**
   - Использование `transform` и `opacity`
   - `will-change` для анимируемых элементов
   - Отключение анимаций на слабых устройствах

---

## Безопасность

### Меры безопасности

1. **Валидация данных**
   - Клиентская валидация форм
   - Проверка типов данных
   - Санитизация пользовательского ввода

2. **Обработка ошибок**
   - Не раскрывать внутренние ошибки
   - Понятные сообщения для пользователя

3. **XSS защита**
   - React автоматически экранирует HTML
   - Использование `dangerouslySetInnerHTML` только при необходимости

---

## Заключение

Приложение "Каталог книг" представляет собой полнофункциональное веб-приложение с современной архитектурой, продуманным UX и производительностью. Все компоненты спроектированы с учетом переиспользования, масштабируемости и поддержки.

